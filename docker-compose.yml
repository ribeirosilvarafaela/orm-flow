services:
  omr-app:
    build:
      context: .
      dockerfile: docker/Dockerfile
    container_name: omr-notes-extractor
    ports:
      - "8000:8000"
    volumes:
      - ./app:/app/app
      - ./data:/app/data
    environment:
      - PYTHONPATH=/app
    restart: unless-stopped
